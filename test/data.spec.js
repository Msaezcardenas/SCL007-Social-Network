require('../src/js/data.js');

// EJEMPLO DE TEST

describe('Indicar el título del test o Resultado que entregará', () => {
  it('is a function', () => {
    expect(typeof updateDelete).toBe('undefined');
  });
  // it('returns `indicar qué retorna`', () => {
  //   expect(updateDelete()).toContainEqual("indicar un valor contenido en el retorno"]);
  // });
});
